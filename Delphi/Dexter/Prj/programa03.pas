// Isto é um comentario e devera ser ignorado
// comentario
// comentario
// comentario
program programa03.pas;
var
  Sim: Boolean;  // Variavel booleana
begin
  WriteLn('Testar instrucao IF:');
  Sim:= true;
  Write('Valor Sim: '); WriteLn(sim);
  if Sim then
  begin 
    Sim:= false;
    Write('Entrou sim: '); WriteLn(sim);
  end;
  //else 
  //  Sim:= true;

  Write('Valor Sim: '); WriteLn(sim);  
  if Sim then
  begin 
    Sim:= true;
    Write('Entrou sim: '); WriteLn(sim);
  end;

  begin
    sim:= false + true + 5;
  end;

  Write('Valor do sim agora eh '); WriteLn(sim);  

end.