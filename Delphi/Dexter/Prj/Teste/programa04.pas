program Programa04;
var
  Soma, Total: Integer;
  msg: string; 
  X: Integer;	
  x2, x1: Integer;
  x3, x4: Integer;
begin
  WriteLn(1 = 1);
  WriteLn('Teste');
  WriteLn(50+4*2);
  soma:= 4+4;
  Write('4 + 4 = ');
  WriteLn(soma);
  Total:= 1;
  WriteLn(Total + Soma);
  Total:= 50 ;
  WriteLn(Total);
  Total:= Total + 1 + 1;
  WriteLn(Total);
  x:= total - soma + 1;  
  WriteLn(X);
  x1:= X;
  x2:= x1;
  Writeln(x2);
  Write('Informe um valor para x2: ');
  Readln(x2);
  Writeln(x2+2);
  x2:= 10; x1:= 10;
  WriteLn(x2 + x1 * 2 + 1);
  x3:= 10;
  x3:= x3 + 10;
  WriteLn(x3);
end.